<% @orders.each do |order| %>
  <tr>
    <td><%= order.order_number %></td>
    <td><%= order.customer_name %></td>
    <td><%= order.status %></td>
    <td><%= link_to "Détails", admin_order_path(order) %></td>
  </tr>
<% end %>